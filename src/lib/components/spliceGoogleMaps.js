// TODO: Refactor this component name
import React from 'react';

import Container from './container';

const SpliceGoogleMaps = (props) => <Container {...props} />;

export default SpliceGoogleMaps;
